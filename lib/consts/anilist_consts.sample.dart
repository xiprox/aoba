/// Sensitive constants related to AniList (such as client ID). If you update
/// this file, remember to update it on GitHub Actions secrets as well.
@Deprecated('DO NOT USE. THIS IS SAMPLE CODE. Make a copy and add real values')
class AnilistConsts {
  /// Client ID used for mobile authentication (redirects to the app).
  static const clientId = 0000;

  /// Client ID used for desktop authentication (gives access token in the
  /// browser window).
  static const clientIdPin = 0000;
}
