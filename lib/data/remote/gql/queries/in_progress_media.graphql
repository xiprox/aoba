query FetchMedia($id: Int!) {
  Media(id: $id) {
    id
    title {
      romaji
      english
      native
      userPreferred
    }
    format
    status
    description
    startDate {
      year
      month
      day
    }
  }
}